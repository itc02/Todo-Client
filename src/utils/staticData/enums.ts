export enum routes {
  server = 'http://localhost:3000',
  todos = 'todos',
  users = 'users'
};

export enum labels {
  title = 'New title',
  deadline = 'End date',
  assignTo = 'Assign to',
  description = 'New description',
  user = 'New user',
  email = 'New email',
  filter = 'Filter by = ',
  state = 'New state'
};

export enum dateFormats {
  moment = 'MM/DD/YYYY',
  datePicker = 'MM/dd/yyyy'
};

export enum buttons {
  cancel = 'Cancel',
  delete = 'Delete'
}

export enum helpers {
  symbol = 'Symbols'
}
